import { ModulePage } from '@/components/layout/ModulePage';

export default function StagesPage() {
  return (
    <ModulePage
      eyebrow="Content"
      title="Quản lý giai đoạn"
      description="CRUD stages theo period, reorder, preview timeline item, mở nhanh events và sync stage sang graph."
      phase="Phase 1"
    />
  );
}
